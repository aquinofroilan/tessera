sonar.projectKey=aquinofroilan_zentry
sonar.organization=aquinofroilan

sonar.sources=app,components,lib
sonar.exclusions=node_modules/**,.next/**,build/**

# Duplication exclusions: seed/mock fixtures repeat row shapes by design.
# Drop these entries once the real API client replaces _data/mock.ts.
sonar.cpd.exclusions=**/_data/mock.ts,**/_data/*.mock.ts
